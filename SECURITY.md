# 보안 — 먼저 읽어주세요

**이 기기에 실제 자금을 넣지 마세요.** 기본 네트워크는 **Base Sepolia**(chainId 84532)와
**Solana Testnet** 이고, SDK 에 메인넷 설정을 일부러 넣지 않았습니다. 해커톤 프로토타입이고, 상용 하드웨어
지갑이 가진 방어 수단 대부분이 없습니다. 무엇이 없는지 정확히 적어둡니다.

---

## 1. 키 추출 — 가장 큰 문제

nRF52840 에는 **보안 요소(Secure Element)가 없습니다.** 개인키는 내부 플래시에
평문으로 있고, 다음이 전부 사실입니다.

- **디버그 포트.** `APPROTECT` 를 켜지 않으면 SWD 로 플래시를 통째로 읽어갈 수
  있습니다. 보드에 Cortex Debug 커넥터가 노출되어 있습니다.
- **APPROTECT 우회.** 켜더라도 nRF52 시리즈의 `APPROTECT` 는 **전압 글리칭으로
  우회 가능하다는 것이 공개된 취약점**입니다 (LimitedResults, 2020). 장비값
  수십 달러 수준입니다. 이건 소프트웨어로 못 막습니다.
- **부트로더.** Adafruit nRF52 부트로더는 UF2/DFU 로 임의 펌웨어를 올릴 수
  있습니다. 서명 검증 없이 올라간 펌웨어가 키를 그대로 읽어 내보낼 수 있습니다.

→ **기기를 물리적으로 손에 넣은 공격자는 키를 가져갑니다.** Ledger 의 ST33 같은
CC EAL5+ 보안 요소가 막는 것이 정확히 이 부분이고, 여기엔 그게 없습니다.

## 2. 블라인드 서명 — 두 번째 문제

기기에 **화면이 없습니다.** LED 4개로는 "0.5 ETH 를 0xAbC… 로 보낸다" 를 보여줄
수 없습니다. 하드웨어 지갑의 핵심 가치는 *what you see is what you sign* 인데,
그게 성립하지 않습니다.

버튼 챌린지는 **"사람이 물리적으로 여기 있다"만 증명합니다.** 사용자가 승인하는
내용이 화면에 뜬 것과 같은지는 증명하지 못합니다. 호스트 PC 가 감염되면
화면에는 "0.01 ETH 전송" 을 띄우고 기기에는 "전액 전송" 을 보낼 수 있고,
사용자는 구분할 방법이 없습니다.

완화책으로 넣은 것 — 근본 해결은 아닙니다.

- 기기가 호스트가 준 해시를 그대로 서명하지 않습니다. **RLP 원문을 받아 기기가
  직접 Keccak-256 을 계산**합니다. 해시만 받으면 무엇이든 서명하게 됩니다.
- 챌린지 시퀀스는 **BLE 로 절대 나가지 않습니다.** LED 로만 표시합니다.
- 요청은 한 번에 하나만 처리합니다.

## 3. 니모닉이 BLE 로 나갑니다

`SETUP_GENERATE` 는 생성한 니모닉을 **BLE 로 평문 전송**합니다. 화면이 없어서
달리 사용자에게 보여줄 방법이 없기 때문입니다.

- BLE 링크 자체는 본딩 + 암호화가 걸립니다.
- 그래도 **호스트 PC 가 니모닉을 봅니다.** PC 가 감염되어 있으면 끝입니다.
- 실제 제품은 니모닉이 기기 밖으로 나가면 안 됩니다.

오프라인에서 만든 니모닉을 `SETUP_RESTORE` 로 넣는 쪽이 그나마 낫습니다.

## 3-1. PIN 과 저장된 니모닉

니모닉은 플래시에 **암호화해서** 넣습니다. 키는 PIN 에서 뽑습니다.

```
K     = PBKDF2-HMAC-SHA512(PIN, SALT(16바이트 랜덤), 4096회)
kenc  = K[0..32)        키스트림용
kmac  = K[32..64)       인증용
CT    = 워드인덱스 XOR HMAC-SHA256(kenc, SALT ‖ be32(블록번호))
MAC   = HMAC-SHA256(kmac, 헤더 ‖ CT)
```

표준 AEAD 가 아니라 HMAC/SHA-2 만으로 만든 encrypt-then-MAC 입니다. 이미 있는
원시 함수만 쓰려고 그렇게 했습니다. **이것이 막는 것과 못 막는 것을 정확히
적습니다.**

| | |
|---|---|
| 막는 것 | 플래시를 그냥 읽어서 니모닉을 바로 보는 것 |
| 막는 것 | 저장된 레코드를 조작해 다른 지갑으로 바꿔치기하는 것 (MAC) |
| **못 막는 것** | **오프라인 무차별 대입.** PIN 공간이 4자리면 4⁴ = 256, 8자리라도 4⁸ = 65,536 입니다. 플래시를 덤프한 공격자는 PC 에서 전부 시도합니다. PBKDF2 4096회로는 몇 초면 끝납니다 |
| 못 막는 것 | 재시도 제한(5회)은 **기기 안에서만** 유효합니다. 플래시를 뜯어낸 뒤에는 의미가 없습니다 |

- PIN 을 설정하지 않으면 길이 0 의 PIN 으로 같은 과정을 밟습니다. 즉 키가
  공개값이고, 플래시를 덤프하면 그대로 열립니다.
- **PIN 길이는 평문으로 저장됩니다** (설정 웹이 몇 자리를 눌러야 하는지 알아야
  하기 때문). 값 자체는 저장하지 않습니다 — 저장하는 건 MAC 뿐입니다.
- PIN 을 설정·변경할 때 **PIN 이 BLE 로 평문 전송**됩니다. 기기에 입력 UI 가
  없어서입니다. 반면 **잠금 해제할 때의 PIN 입력은 기기 버튼으로만** 받으며
  BLE 로 나가지 않습니다.

→ **PIN 은 기기를 주운 사람이 그냥 켜서 서명하는 것을 막는 장치입니다.**
플래시를 뜯을 능력이 있는 공격자에게는 의미가 없습니다. 그 경우 자금을 지키는
것은 **BIP-39 패스프레이즈뿐**입니다 — 기기에 저장되지 않고 잠금 해제할 때마다
호스트가 넣기 때문입니다. §7 참고.

## 4. BLE

- **본딩을 강제합니다.** 페어링 안 된 연결은 어떤 명령도 처리하지 않습니다.
- LE Secure Connections 를 씁니다. 다만 **버튼 4개·LED 4개로는 숫자 비교
  (Numeric Comparison) 를 제대로 못 하므로 Just Works 로 떨어집니다.**
  Just Works 는 MITM 을 막지 못합니다 — 최초 페어링 시점에 공격자가 끼어들 수
  있습니다. 신뢰할 수 있는 환경에서 페어링하세요.

## 5. 챌린지의 실제 강도

4버튼 × 4단계 = 256 조합. **이건 비밀이 아닙니다** — 기기가 LED 로 알려줍니다.
목적은 추측 방지가 아니라 **물리적 존재 증명**입니다. 원격 공격자는 버튼을 누를
수 없습니다. 3회 실패 시 요청을 폐기하는 건 오조작 방지용입니다.

어깨너머로 보는 공격에는 의미가 없지만, 매번 시퀀스가 바뀌므로 **재생 공격은
불가능**합니다.

## 6. 그래서 뭘 배우는가

이 프로젝트가 제대로 다루는 것:

- BIP-39 / BIP-32 / secp256k1 을 MCU 에서 구현
- 키를 호스트로 내보내지 않고 기기 안에서만 쓰는 구조
- BLE 위에 프레이밍 + 명령 프로토콜 설계
- 사람의 물리적 승인을 서명 경로에 강제로 끼워넣는 방법
- DApp 이 쉽게 붙일 수 있는 SDK 경계 설계

다루지 못하는 것: **물리 공격 내성, 신뢰할 수 있는 표시 장치.** 이 둘이
하드웨어 지갑을 하드웨어 지갑으로 만드는 부분이고, 이 하드웨어로는 안 됩니다.

## 7. 그래도 조금 낫게 하려면

| 조치 | 효과 |
|---|---|
| `APPROTECT` 활성화 | 캐주얼한 SWD 덤프 차단. 글리칭은 못 막음 |
| 니모닉을 오프라인 생성 후 `SETUP_RESTORE` | PC 가 니모닉 전체를 보지 않음 |
| BIP-39 패스프레이즈 추가 | 플래시를 덤프당해도 자금은 지킴. **강력히 권장** |
| 테스트넷만 사용 | 손실 자체를 없앰. **기본 설정이 이미 테스트넷 전용이다** |
| 소액 전용 지갑으로 한정 | 피해 상한 설정 |

**BIP-39 패스프레이즈가 이 하드웨어에서 가장 비용 대비 효과가 큽니다.**
패스프레이즈는 기기에 저장되지 않고 서명할 때마다 호스트가 넣으므로, 플래시를
통째로 털려도 자금은 남습니다. `SETUP_RESTORE` 와 서명 명령에서 지원합니다.
