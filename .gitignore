# dependencies
/node_modules
**/node_modules/
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*

# deployment and generated files
.vercel
next-env.d.ts
/dist/
**/dist/
/.wrangler/
/outputs/
/work/

# TypeScript
*.tsbuildinfo

# firmware build artifacts
# 디렉터리를 통째로 무시하면 안쪽 파일을 ! 로 되살릴 수 없다. 내용만 무시한다.
/firmware/build/*
# 설치 문서(docs/nu40-dk-firmware-installation.md)가 이 파일을 가리키므로 예외
!/firmware/build/NU40PET.UF2
/firmware/wallet/build/

# 호스트 테스트 바이너리와 중간 산출물 (make 로 언제든 다시 만든다)
/firmware/test/test_crypto
/firmware/test/test_wallet
/firmware/test/protocol_bridge
/firmware/test/.obj/

# 펌웨어 빌드 산출물 — 이미 히스토리에 7MB 가량 들어가 있다.
# 보드에 올리는 .uf2 만 남긴다. 설치 문서가 이 파일을 가리키고, 툴체인 없이
# 굽는 유일한 경로다. elf/map/hex/zip 은 언제든 다시 만든다.
/firmware/nuwallet/build/*
!/firmware/nuwallet/build/*.uf2
!/firmware/nuwallet/build/*.UF2
/firmware/build/*.elf
/firmware/build/*.map
/firmware/build/*.zip
/firmware/build/*.hex
